# Dev check for framework installation on the server

## Installation

Require `mbtnet/devcheck` using composer.
Add the `Mbtnet\Devcheck\Providers\DevcheckProvider::class,` entry to the config/app.php file
